## Packages

framer-motion | Essential for smooth micro-interactions, chat message reveals, and panel transitions
date-fns | Required for human-readable chat message timestamps
lucide-react | Icon library (already installed but ensuring it's recognized)

## Notes

- The application defaults to a "Dark Mode Professional" theme.
- Theme customizer dynamically updates CSS custom properties on the `:root` element.
- Video feed requires modern browsers supporting HTML5 autoplay/loop for MP4s.
