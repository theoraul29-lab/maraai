<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Mara AI Chat</title>
<style>
  body {
    margin: 0;
    font-family: Arial, sans-serif;
    transition: background 1s;
    background: #111;
    color: white;
  }

  #chat-container {
    display: flex;
    flex-direction: column;
    height: 100vh;
  }

  #messages {
    flex: 1;
    overflow-y: auto;
    padding: 20px;
  }

  .message {
    margin: 10px 0;
    padding: 10px 15px;
    border-radius: 10px;
    max-width: 70%;
    transition: background 0.5s;
  }

  .user {
    background: #0ff;
    color: #000;
    align-self: flex-end;
  }

  .mara {
    background: #333;
    color: #fff;
    align-self: flex-start;
  }

  #input-container {
    display: flex;
    padding: 10px;
    background: #222;
  }

  #user-input {
    flex: 1;
    padding: 10px;
    border-radius: 6px;
    border: none;
    margin-right: 10px;
    background: #333;
    color: white;
  }

  #send-btn {
    padding: 10px 15px;
    border: none;
    border-radius: 6px;
    background: #0ff;
    color: #000;
    cursor: pointer;
  }
</style>
</head>
<body>
<div id="chat-container">
  <div id="messages"></div>
  <div id="input-container">
    <input type="text" id="user-input" placeholder="Type your message..." autofocus>
    <button id="send-btn">Send</button>
  </div>
</div>

<script>
  const messagesEl = document.getElementById('messages');
  const userInput = document.getElementById('user-input');
  const sendBtn = document.getElementById('send-btn');
  let colors = ['#111', '#1a1a1a', '#222', '#2a2a2a', '#333']; // ambient colors
  let colorIndex = 0;

  function addMessage(text, sender) {
    const msg = document.createElement('div');
    msg.classList.add('message', sender);
    msg.textContent = text;
    messagesEl.appendChild(msg);
    messagesEl.scrollTop = messagesEl.scrollHeight;

    // change ambient background slowly
    document.body.style.background = colors[colorIndex % colors.length];
    colorIndex++;
  }

  function maraReply(userText) {
    // Simulate AI response (you can integrate TTS/AI later)
    let reply = "Mara AI says: I received your message: " + userText;
    
    // Example: simple responses
    if(userText.toLowerCase().includes('trading')) {
      reply = "Mara AI: Trading is buying and selling assets to make profit. I can explain strategies!";
    } else if(userText.toLowerCase().includes('story')) {
      reply = "Mara AI: Let’s create your story! What’s your idea?";
    }

    setTimeout(() => addMessage(reply, 'mara'), 800);
  }

  function sendMessage() {
    const text = userInput.value.trim();
    if(!text) return;
    addMessage(text, 'user');
    userInput.value = '';
    maraReply(text);
  }

  sendBtn.addEventListener('click', sendMessage);
  userInput.addEventListener('keydown', (e) => {
    if(e.key === 'Enter') sendMessage();
  });

</script>
</body>
</html>