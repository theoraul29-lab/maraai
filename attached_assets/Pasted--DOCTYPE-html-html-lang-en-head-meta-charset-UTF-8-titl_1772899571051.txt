<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Mara AI Reels Hub</title>
<style>
body { font-family: Arial; margin:0; background:#111; color:white; overflow-x:hidden; }
#maraBar { display:flex; padding:10px; background:#222; }
#maraInput { flex:1; padding:8px; font-size:16px; }
#maraBtn { padding:8px 12px; margin-left:5px; cursor:pointer; }
#reelsContainer { height:80vh; overflow-y:scroll; background:#000; display:flex; flex-direction:column; align-items:center; scroll-behavior:smooth; }
.reel { margin:10px auto; width:360px; height:640px; border:1px solid #555; cursor:pointer; }
#fullscreenOverlay { display:none; position:fixed; top:0; left:0; width:100vw; height:100vh; background:#000; z-index:1000; justify-content:center; align-items:center; }
#fullscreenOverlay iframe { width:100%; height:100%; }
#backBtn { position:absolute; top:10px; left:10px; z-index:1100; padding:10px 15px; background:#222; color:white; border:none; cursor:pointer; font-size:16px; }
</style>
</head>
<body>

<div id="maraBar">
  <input type="text" id="maraInput" placeholder="Type here to activate Mara...">
  <button id="maraBtn">Activate Mara</button>
</div>

<div id="reelsContainer">
  <!-- Reels dynamically added here -->
</div>

<div id="fullscreenOverlay">
  <button id="backBtn">Back</button>
  <iframe src="" frameborder="0" allowfullscreen allow="autoplay; encrypted-media;"></iframe>
</div>

<script>
const maraBtn = document.getElementById('maraBtn');
const maraInput = document.getElementById('maraInput');
const reelsContainer = document.getElementById('reelsContainer');
const fullscreenOverlay = document.getElementById('fullscreenOverlay');
const backBtn = document.getElementById('backBtn');
const fullscreenIframe = fullscreenOverlay.querySelector('iframe');

// List of YouTube Shorts IDs
const youtubeShorts = [
  "SHORT_VIDEO_ID1",
  "SHORT_VIDEO_ID2",
  "SHORT_VIDEO_ID3"
];

// Function Mara posts a reel
function maraPostReel(videoId){
    const iframe = document.createElement("iframe");
    iframe.className = "reel";
    iframe.src = `https://www.youtube.com/embed/${videoId}?autoplay=1&mute=1&controls=1&loop=1`;
    iframe.allow = "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture";
    iframe.allowFullscreen = true;
    reelsContainer.appendChild(iframe);

    // Click to fullscreen
    iframe.addEventListener('click', ()=>{
        fullscreenIframe.src = `https://www.youtube.com/embed/${videoId}?autoplay=1&mute=0&controls=1&loop=1`;
        fullscreenOverlay.style.display = "flex";
    });
}

// Mara activation
maraBtn.addEventListener('click', ()=>{
    alert("Mara is now active! She can post reels.");
    maraInput.focus();
    youtubeShorts.forEach(id => maraPostReel(id));
});

// Back button from fullscreen
backBtn.addEventListener('click', ()=>{
    fullscreenIframe.src = ""; // stop video
    fullscreenOverlay.style.display = "none";
});

// Optional: Post reel when user types something
maraInput.addEventListener('keypress', (e)=>{
    if(e.key === 'Enter'){
        const text = maraInput.value;
        maraInput.value = '';
        const randomId = youtubeShorts[Math.floor(Math.random()*youtubeShorts.length)];
        maraPostReel(randomId);
        console.log(`Mara posted a reel in response to: "${text}"`);
    }
});
</script>

</body>
</html>