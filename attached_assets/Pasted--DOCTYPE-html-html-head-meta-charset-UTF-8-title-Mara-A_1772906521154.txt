<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Mara AI</title>

<style>

body{
margin:0;
font-family:Arial;
height:100vh;
display:flex;
flex-direction:column;
transition:background 6s ease;
}

/* header */

header{
padding:15px;
font-size:22px;
font-weight:bold;
color:white;
}

/* chat */

#chat{
flex:1;
overflow-y:auto;
padding:20px;
display:flex;
flex-direction:column;
gap:12px;
}

.message{
max-width:60%;
padding:12px;
border-radius:12px;
font-size:15px;
}

.user{
align-self:flex-end;
background:#444;
color:white;
}

.mara{
align-self:flex-start;
background:#ffffff22;
color:white;
}

/* input */

#inputArea{
display:flex;
padding:12px;
}

#textInput{
flex:1;
padding:10px;
border-radius:6px;
border:none;
font-size:15px;
}

button{
margin-left:10px;
padding:10px 16px;
border:none;
border-radius:6px;
background:#ff9966;
color:white;
cursor:pointer;
}

</style>
</head>

<body>

<header>✨ Mara AI</header>

<div id="chat"></div>

<div id="inputArea">
<input id="textInput" placeholder="Scrie mesaj...">
<button onclick="sendMessage()">Trimite</button>
</div>

<script>

/* Detectare zi / noapte */

function isNight(){
const hour = new Date().getHours()
return hour >= 18 || hour < 6
}

/* Palete culori */

const nightColors=[
"#0f0f14",
"#1a1a2a",
"#1b1b30",
"#241c2b",
"#2b1e2b"
]

const dayColors=[
"#f3f6ff",
"#e9f1ff",
"#f6f0ff",
"#fff4e8",
"#eef7ff"
]

/* Schimbare ambient */

function changeAmbient(){

let palette = isNight() ? nightColors : dayColors

let color = palette[Math.floor(Math.random()*palette.length)]

document.body.style.background = color

}

/* chat */

const chat=document.getElementById("chat")

function addMessage(text,type){

let msg=document.createElement("div")
msg.className="message "+type
msg.innerText=text

chat.appendChild(msg)

chat.scrollTop=chat.scrollHeight

}

/* raspuns Mara */

function maraReply(userText){

changeAmbient()

setTimeout(()=>{

addMessage("Mara AI: Am primit mesajul tau.","mara")

changeAmbient()

},1200)

}

/* trimitere */

function sendMessage(){

let input=document.getElementById("textInput")

let text=input.value.trim()

if(!text) return

addMessage(text,"user")

input.value=""

changeAmbient()

maraReply(text)

}

document.getElementById("textInput").addEventListener("keypress",function(e){

if(e.key==="Enter") sendMessage()

})

</script>

</body>
</html>